import React from 'react';
import ReactDOM from 'react-dom/client';
import BotTable from './components/BotTable';
import './components/BotTable';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
        <BotTable />
    </React.StrictMode>
);
